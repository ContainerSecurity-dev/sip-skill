interface:
  display_name: "SIP"
  short_description: "Apply complete SIP supply-chain controls"
  default_prompt: "Use $sip to apply all five SIP supply-chain controls to this Node.js container repository."

policy:
  allow_implicit_invocation: false
