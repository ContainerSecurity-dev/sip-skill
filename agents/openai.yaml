interface:
  display_name: "SIP"
  short_description: "Apply or audit SIP supply-chain controls"
  default_prompt: "Use $sip to apply or audit all five SIP supply-chain controls in this Node.js container repository."

policy:
  allow_implicit_invocation: false
